# Railway: mount a persistent volume at /data and set DATA_DIR=/data
# so workout minutes, partners, and tokens survive deploys.
[build]
builder = "NIXPACKS"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
